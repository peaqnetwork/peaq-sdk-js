{
  "name": "peaq-network",
  "description": "SDK Monorepo for the peaq network.",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "npm run build --workspaces",
    "typecheck": "tsc -b --pretty false",
    "clean": "rimraf packages/*/dist",
    "lint": "eslint ."
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "eslint": "^9.0.0",
    "rimraf": "^6.0.0",
    "tsup": "^8.5.0"
  },
  "overrides": {
    "tslib": "^2.6.3"
  },
  "engines": { "node": ">=18" }
}