export enum ConfirmationMode {
    FAST = 'FAST',
    CUSTOM = 'CUSTOM',
    FINAL = 'FINAL'
}

export enum TransactionStatus {
    BROADCAST = 'BROADCAST',
    IN_BLOCK = 'IN_BLOCK',
    FINALIZED = 'FINALIZED'
}